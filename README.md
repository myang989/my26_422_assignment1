In order to run the automated scripts:

For part 1, run:
```bash
python3 ping_rtt.py
```
which generates the scatter plot.

For part 2, run:
```bash
python3 latency.py
```
which generates the stacked bar graph and scatter plot.